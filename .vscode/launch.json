{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/src/main",
            "args": [],
            "preLaunchTask": "${defaultBuildTask}",
        }
    ],
    "version": "0.2.0",
}